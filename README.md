The codes are for the simulation of the Continuous Axelrod model. One code is for sigma=0 (no noise) whereas the other code is for the general case (noise).
Both codes are compiled in this way: c++ -O3 name.cpp -o name -lm -lgsl -lgslcblas
